describe("PubSubManager", () => {
  it("should create and sub a channel when subscribed to", () => {});
  it("should sub a channel when it already exists", () => {});
  it("should create and sub a pattern when subscribed to", () => {});
  it("should sub a pattern when it already exists", () => {});
  it("should publish in a channel and notify subscribers", () => {});
  it("should publish in a channel and notify pattern subscribers", () => {});
});
